{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the SpectrEm main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:menu:default",
    "core:app:default",
    "core:window:default",
    "dialog:default",
    "fs:default",
    "opener:default",
    "opener:allow-open-url"
  ]
}
